<!-- Navbar-->
<nav class="navbar navbar-expand-lg shadow navbar-light bg-white">
  <div class="container-fluid">
    <div class="d-flex align-items-center"><a class="navbar-brand py-1"></a>
        <a class="navbar-brand" style="font-family: 'Sansita Swashed', cursive; font-size: 1.8rem" href="/">Clap for Life</a>
    </div>
    <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation"><i class="fa fa-bars"></i></button>
    <!-- Navbar Collapse -->
    <div class="collapse navbar-collapse" id="navbarCollapse">
      <ul class="navbar-nav ml-auto">
        <%if user_signed_in?%>
          <div class="nav-item dropdown">
            <a class="btn btn-primary dropdown-toggle pr-3" href="#" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" style="color:white">
              <%= current_user.username %>
            </a>
              <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
                <a class="dropdown-item" <%= link_to "Mon profil", user_path(current_user.id) %></a>
                <a class="dropdown-item" <%= link_to "Se déconnecter", destroy_user_session_path, method: :delete %></a>
              </div>
          </div>

        <%else%>
          <ul>
            <li class="nav-item mt-3 mt-lg-0 ml-lg-3 d-lg-none d-xl-inline-block"><a class="nav-link" <%= link_to "Se connecter", new_user_session_path %></a></li>
            <li class="nav-item mt-3 mt-lg-0 ml-lg-3 d-lg-none d-xl-inline-block"><a class="btn btn-primary" <%= link_to "S'inscrire", new_user_registration_path %></a></li>
          </ul>

        <%end%>
      </ul>
    </div>
  </div>
</nav>
<!-- /Navbar -->
